//Numero divisble por 5

let numero = prompt("Ingrese el numero") ; // le asignamos al numero a dividir la variable de numero

let division = numero %5; // por medio del modulo realizamos la divicion.
if (division==0){ //si el resultado del modulo es 0 sera verdadero y por lo tamto imprimira "es divicible por 5"
    console.log("es divisible por 5"); //en caso de que el resultado de la modulocacion sea diferente a 0 no imprimira nada
    
}