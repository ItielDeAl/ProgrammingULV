//Compara si la cadena inicia con a 

let texto = prompt("Ingrese texto");

if (texto.startsWith('A')){//Evaluamos si la ca cadena inicia con A
    console.log("La cadena inicia con A"); //Si la comparacion es verdadera se imprimira el texto
}
if (texto.startsWith('a')){ //Evaluamos si la ca cadena inicia con a
    console.log("La cadena inicia con a"); //Si la comparacion es verdadera se imprimira el texto
}
